# HRC2026 Object Detection

Workspace for Task A - Dataset + Frame preparation is kept separate from the
existing `data/` folder:

```text
task_A_dataset_frame/
```

Use this folder for audit logs, extraction runbooks, manifests, hard-case lists,
and future frame outputs for labeling/training preparation.
